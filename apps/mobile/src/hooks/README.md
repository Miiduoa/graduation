# hooks

- `useDataSource()` – returns current data source (`mock` / `firebase` / `hybrid`); hybrid is the long-term integration target
- `useAsyncList(load)` – tiny helper to load list data with loading/error state
